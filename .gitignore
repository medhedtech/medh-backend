node_modules/
# Environment variables (CRITICAL - NEVER COMMIT)
.env
.env.*
!.env.example
.vercel

# AWS credentials
aws-credentials.txt
aws-config.txt

# Ignore all files in logs directory but keep the directory structure
logs/*
!logs/.gitkeep

# Specific log file patterns to ignore
*.log
*.log.*
logs/*.log
logs/*.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Output of 'npm pack'
*.tgz

# Optional npm cache directory
.npm

# SSH keys
eval "$(ssh-agent -s)"
eval "$(ssh-agent -s)".pubtest-video-lesson-fix-2.js
